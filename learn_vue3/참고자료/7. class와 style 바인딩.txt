HTML 클래스 바인딩
 - 객체 바인딩
   - 클래스를 동적으로 바인딩 하기위해서는 :class(v-bind:class)를 사용할 수 있다.
   - :class와 일반 class속성과 공존할 수 있다. 그리고 개체를 반환하는 computed에 바인딩 할 수도 있다.
   - Object, 2개 이상도 바인딩이 가능하다.
 - style 바인딩
   - style 속성도 바인딩(:style)이 가능하다. 
   - 하지만 inner styling은 카멜표기법으로 해야된다.  