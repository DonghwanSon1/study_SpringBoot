merge보다 변경감지를 활용하자

가장 좋은 방법 => 엔티티를 변경할 때는 항상 변경 감지를 사용해라
- 컨트롤러에서 어설프게 엔티티를 생성하지 마라
- 트랜잭션이 있는 서비스 계층에 식별자('id')와 변경할 데이터를 명확하게 전달해라 (파라미터 or Dto)
- ★트랜잭션이 있는 서비스 계층에서 영속 상태의 엔티티를 조회하고, 엔티티의 데이터를 직접 변경해라
- 트랜잭션 커밋 시점에 변경 감지가 실행된다.










